apply plugin: "java"
apply plugin: "idea"
apply plugin: "eclipse"

group = "solar.dimensions.api"
version = "0.1.0"

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.google.code.gson:gson:2.3.1'
}